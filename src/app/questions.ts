export interface IQuestions {
  category: string;
  questions: string;
  difficulty: string;
  questionid: string;
  isEnabled: string;
  correctchoices: number;
  description: string;
  text: string;
}
